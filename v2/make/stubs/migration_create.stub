package migrations

import (
	"{{ModelsImportPath}}"
	"gorm.io/gorm"

	"github.com/gtkit/migrate/v2/migration"
)

func init() {
	up := func(db *gorm.DB) error {
		return db.Migrator().CreateTable(&{{ModelPackageName}}.{{StructName}}{})
	}

	down := func(db *gorm.DB) error {
		return db.Migrator().DropTable(&{{ModelPackageName}}.{{StructName}}{})
	}

	migration.Add("{{FileName}}", up, down)
}
